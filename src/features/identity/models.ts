/**
 * URL 身份识别模块（向后兼容重导出）
 *
 * URL handling logic has moved to @/shared/identity.ts.
 * This file re-exports for backward compatibility — all imports
 * via '@/features/identity' continue to work without changes.
 */

export { Identity } from '@/shared/identity'
